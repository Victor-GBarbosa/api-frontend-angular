import { Component } from '@angular/core';
import { Header } from '../../components/header/header';

@Component({
  selector: 'app-cart',
  imports: [Header],
  templateUrl: './cart.html',
  styleUrl: './cart.scss',
})
export class Cart {}
